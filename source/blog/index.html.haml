---
title: A blog about coding apps, loving life and board sports
pageable: false
---

%section
  %ul.articles
    - previous_year = 0
    - blog.articles.each do |article|
      %li
        .year
          - if previous_year != article.date.year
            - previous_year = article.date.year
            = article.date.year
          - else
            &nbsp;
        .date
          = article.date.strftime('%b %e')
        .post
          %h2= link_to article.title, article
          %em posted on
          %ul.tags
            - article.tags.each do |tag|
              %li
                = link_to tag, tag_path(tag)


